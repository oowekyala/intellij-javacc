jjtx:
  nodeGen:
    myGen:
      "r:.*":
        - template: |
            #if( !package.empty )
            package $package;
            #end
            public class $simpleName extends ${node.class.qualifiedName} {

            }
          formatter: "none"

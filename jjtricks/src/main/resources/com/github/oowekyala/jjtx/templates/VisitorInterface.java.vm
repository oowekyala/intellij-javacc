#evaluate( $!copyrightHeader )
#evaluate( $!packageDecl )
#evaluate( $!versionSpecificDecls )

$!visibility $!modifiers interface ${thisClass.simpleName}#evaluate($!generics) {

    #foreach($node in $grammar.typeHierarchy)

        #evaluate($methodTemplate)

    #end

}
